apt update;
apt install build-essential --yes;    # Основные пакеты для сборки
apt install docker.io --yes;          # Демон и клиент Docker
apt install docker-compose --yes;     # Консольная утилита для управления множеством Docker контейнеров
apt install docker-compose-plugin     # Поддержка compose в Docker CLI